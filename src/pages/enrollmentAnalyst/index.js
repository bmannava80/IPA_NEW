import AppLayout from '../../layouts/appLayout';
import { tabs } from '../../utils/tabs';

export default function EnrollmentAnalyst() {
  return <AppLayout tabs={tabs.enrollmentAnalyst}></AppLayout>;
}
